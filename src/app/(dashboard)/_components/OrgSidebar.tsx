'use client'

interface OrgSidebarProps { }

export function OrgSidebar({ }: OrgSidebarProps) {
    return (
        <div className="hidden lg:flex flex-col w-[206px] pl-5 pt-5 space-y-6">
            OrgSidebar
        </div>
    )
}
